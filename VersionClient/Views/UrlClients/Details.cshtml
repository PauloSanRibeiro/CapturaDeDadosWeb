@model VersionClient.Models.UrlClient

@{
    ViewData["Title"] = "Endereço de Acesso";
}

<h2 align="center">@ViewData["Title"]</h2>

<hr />

<div>

    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Address)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.IdUrlClient" class="btn btn-success">Editar</a>
    <a asp-action="Index" class="btn btn-info">Voltar</a>

</div>